import React from "react";

export default function GenerateButton(props) {
    return <div>
        <button type="button" id="generate_button" className="bg-red-800 p-2.5 rounded text-sm font-semibold text-gray-200">
            Generate
        </button>
    </div>
}